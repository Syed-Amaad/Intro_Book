//3
// The code outputs an error due to the variable's block scope.

//4
// The code outputs an error when trying to reassign the constant variable.

//5
// The code logs 'bar' because the variable within the block scope is inaccessible.

//6 
// The program doesn't produce an error, because the "new" constant variable is inaccessible from outside the function block.