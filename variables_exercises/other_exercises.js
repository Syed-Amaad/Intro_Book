/*

(3)
The code outputs an error due to the variable being out of scope.

(4)
The program runs the first part, then outputs an error when trying to reassign the constant variable.

(5) 
The code logs 'bar' because the variable that is declared within the block scope is inaccessible.

(6) 
The program doesn't produce an error, because a new constant variable was declared within the function block.

*/